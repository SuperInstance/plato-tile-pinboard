[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-pinboard"
version = "0.1.0"
description = "Pin important tiles — categories, notes, expiry, search"
license = {text = "MIT"}
requires-python = ">=3.8"
